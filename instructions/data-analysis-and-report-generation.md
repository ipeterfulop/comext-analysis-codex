# Data Analysis & Report Generation

This document has been merged into `AGENTS.md`.

Use `AGENTS.md` as the single source of truth for:

- the COMEXT data-preparation pipeline
- Slidev report-generation rules
- required subagent outputs
- runtime and assembly behavior

This file is kept only as a compatibility pointer to avoid duplicate agent instructions.
